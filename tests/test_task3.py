# tasks/task3.py

    def solve():
    try:
        # Считываем строку и разбиваем её на список строк
        line = input().split()
        
        # Если строка пустая, выходим из функции
        if not line:
            return
            
        # Преобразуем первые два элемента в целые числа
        # a - прострелено Гарри, b - прострелено Ларри
        a = int(line[0])
        b = int(line[1])

        # Расчет по логике: общее кол-во банок = a + b - 1
        # Гарри не прострелил: (a + b - 1) - a = b - 1
        # Ларри не прострелил: (a + b - 1) - b = a - 1
        harry_missed = b - 1
        larry_missed = a - 1

        # Выводим результат строго в формате: число число
        print(f"{harry_missed} {larry_missed}")
        
    except (ValueError, IndexError):
        # В случае ошибки ввода ничего не выводим, чтобы не ломать тесты
        pass

# Этот блок должен быть на уровне края файла (без отступов)
if __name__ == "__main__":
    solve()
