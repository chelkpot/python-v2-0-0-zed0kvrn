# tasks/task1.py

    def solve():
    # Считываем входное число S
    # int(input()) преобразует введенную строку в целое число
    S = int(input())

    # По логике задачи:
    # Петя (p) = S // 6
    # Сережа (s) = p
    # Катя (k) = (p + s) * 2 = 4 * p
    p = S // 6
    k = 4 * p
    s = p

    # Выводим результат в одну строку через пробел
    # Функция print автоматически ставит пробелы между аргументами
    print(p, k, s)

# Код ниже не трогать! Он необходим для запуска тестов
if __name__ == "__main__":
    solve()
